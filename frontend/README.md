# Frontend（Vue3 + Vite）

## 启动

```bash
cd frontend
npm install
npm run dev
```

## 构建

```bash
npm run build
```

## 环境变量

- `VITE_API_BASE_URL`：后端 API 地址
- `VITE_BASE_PATH`：部署在 GitHub Pages 子路径时使用（如 `/repo-name/`）

